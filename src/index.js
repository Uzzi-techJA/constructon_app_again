import React from 'react';
import ReactDOM from 'react-dom/client';
import ConstructionApp from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ConstructionApp />
  </React.StrictMode>
);
